# Leave both unset for local development -- the app falls back to an
# embedded SQLite file at data/gym.db automatically.
#
# For production (Vercel), create a free Turso database and set these
# as Vercel environment variables. See README.md > "Deploy for free"
# for the exact commands.

TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
